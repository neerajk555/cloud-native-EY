# Lab 1.2: Explore Your Guardrails

**Cost:** ✅ Free
**Time:** ~15 min

## Objective

Read PERMISSIONS-AND-GUARDRAILS.md and confirm a denied action actually gets denied, so you recognize it later.

## Prerequisites

- Lab 1.1 complete
- `$PARTICIPANT` is exported for this session (see `SETUP-UBUNTU.md`)

## Steps

### Step 1: Try an oversized instance (expect denial)

```bash
aws ec2 run-instances --image-id ami-0abcdef1234567890 --instance-type m5.large --count 1
```
This is expected to fail — that's the point.

### Step 2: Read the denial reason

```bash
# Look for 'explicit deny' in the error and the Sid it names
```


## Expected Result

The call fails with an explicit deny referencing the `DenyOversizedEc2` statement, not a generic permissions error.

## Cleanup

```bash
Nothing created (the call was denied).
```

## Troubleshooting

- **Call succeeds unexpectedly**: Stop and flag this to the instructor immediately — it means the boundary isn't attached correctly.
