# Lab 11.1: Caching (Instructor-Led ElastiCache Demo + Your Local Redis)

**Cost:** ✅ Free for participants (local Docker Redis); instructor demo uses ElastiCache
**Time:** ~15 min

## Objective

Implement cache-aside caching against a local Docker Redis container while the instructor demonstrates the same pattern on ElastiCache.

## Prerequisites

- Module 3 complete
- `$PARTICIPANT` is exported for this session (see `SETUP-UBUNTU.md`)

## Steps

### Step 1: Watch the instructor's ElastiCache demo

```bash
# No action — instructor-led
```


### Step 2: Run Redis locally

```bash
docker run -d --name redis-$PARTICIPANT -p 6379:6379 redis:7
```


### Step 3: Implement cache-aside against it

```bash
# See sample app: check cache, on miss query 'DB', populate cache
```


## Expected Result

A second identical request is served from cache (visibly faster, and Redis shows a hit).

## Cleanup

```bash
docker rm -f redis-$PARTICIPANT
```

## Troubleshooting

- **Why not real ElastiCache for me?**: ElastiCache cost scales per node, not per hour of use — 25 nodes would multiply cost for no added learning value. See SYNC-NOTES.md Amendment 3.
