# Lab 2.2: Synchronous Service-to-Service Calls

**Cost:** ✅ Free Tier
**Time:** ~15 min

## Objective

Call one Lambda function from another to model synchronous microservice communication.

## Prerequisites

- Lab 2.1 complete
- `$PARTICIPANT` is exported for this session (see `SETUP-UBUNTU.md`)

## Steps

### Step 1: Deploy a second function

```bash
aws lambda create-function --function-name downstream-$PARTICIPANT --runtime nodejs20.x --handler index.handler --role $roleArn --zip-file fileb://fn.zip --tags Owner=$PARTICIPANT,Course=cloudnative-agentic-ai-2026
```


### Step 2: Invoke it from the first (conceptually, via AWS SDK inside a handler)

```bash
# See lab code sample for the aws-sdk invoke() call pattern
```


## Expected Result

The upstream function's response includes the downstream function's payload.

## Cleanup

```bash
aws lambda delete-function --function-name downstream-$PARTICIPANT
```

## Troubleshooting

- **Throttling**: Free tier concurrency is shared account-wide across 25 participants — retry with backoff.
